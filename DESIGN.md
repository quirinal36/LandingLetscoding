# 렛츠코딩 디자인 시스템

**그림자를 쓰지 않는다.** `box-shadow`는 이 프로젝트 어디에도 없다. 그라디언트도 쓰지 않는다.
면은 단색이고, 깊이는 다음 세 가지로만 만든다.

1. **면의 밝기 단계** — 회색 바탕 위의 흰 면이 곧 "올라온 것"이다.
2. **헤어라인** — 1px 선이 경계를 긋는다.
3. **반투명 + 블러** — 떠 있는 것은 아래를 비친다. iOS·macOS의 내비게이션 바와 메뉴가 그림자 없이 떠 보이는 방식이 이것이다.

## 원칙

1. **위계는 색이 아니라 면의 단계가 만든다.** 카드는 흰색, 바탕은 회색. 그 차이 하나가 카드를 띄운다.
2. **선은 "누를 수 있음"의 신호다.** 컨트롤에만 긋고 카드에는 긋지 않는다. 누를 수 없는 것까지 테두리를 그리면 구분이 사라진다.
3. **색은 한 자리에만.** 포인트 블루는 전환 동선(설명회·도입문의)과 현재 위치 표시에만. 나머지는 전부 무채색이다.
4. **누르면 면이 한 단계 어두워진다.** 투명도를 낮추지 않는다.
5. **모션은 입력에만 반응한다.** 스크롤 등장 연출 없음. 누름은 150ms.
6. **장식이 없을수록 글자가 위계를 짊어진다.** 크기와 굵기의 간격을 넉넉히 벌린다.

## 토큰

`src/app/globals.css`의 `@theme` 블록이 유일한 출처다.

| 토큰 | 값 | 쓰이는 곳 | 대비 |
|---|---|---|---|
| `--color-bg` | `#e9edf3` | 페이지 바탕 | — |
| `--color-surface` | `#ffffff` | 카드 · 메뉴 · 컨트롤 | — |
| `--color-fill` | `#dde3ec` | 트랙 · 눌린 상태 | — |
| `--color-fill-strong` | `#d0d8e4` | 꺼진 표시등 | — |
| `--color-ink` | `#1b2333` | 본문 | 13.4:1 |
| `--color-ink-soft` | `#55607a` | 보조 설명 | 5.3:1 (흰 면 6.2:1) |
| `--color-ink-faint` | `#5e6880` | 캡션 | 4.7:1 (흰 면 5.6:1) |
| `--color-separator` | `#d5dbe4` | 구분선 · 떠 있는 면의 테두리 | — |
| `--color-edge` | `#7c8699` | **컨트롤 경계 전용** | 3.1:1 / 흰 면 3.7:1 |
| `--color-accent` | `#3b6fe0` | 포인트 컨트롤 (흰 글자 4.6:1) | — |
| `--color-accent-ink` | `#2a53b0` | 면 위의 포인트 **글자** | 6.0:1 |

포인트 블루를 면 위에 글자로 쓸 때 `--color-accent`를 그대로 쓰면 3.9:1로 미달이다.
`--color-accent-ink`를 쓴다.

## 클래스

| 클래스 | 뜻 |
|---|---|
| `.card` | 흰 면. 바탕과의 밝기 차이만으로 떠 보인다 |
| `.track` | 들어간 자리. 세그먼트 트랙 |
| `.floating` | 떠 있는 면. 반투명 + 블러 + 헤어라인. 블러 미지원 브라우저에서는 불투명 흰 면으로 떨어진다 |
| `.bar` | 상단 바. 스크롤하면 `data-scrolled`로 헤어라인 한 줄이 그어진다 |
| `.control` / `.control-accent` | 누를 수 있는 것. 헤어라인 경계 포함 |
| `.segment` | 글자 자체가 어포던스인 항목. `data-selected`면 흰 면으로 올라온다 |
| `.menu-row` | 메뉴 한 줄 |
| `.dot` | 현재 위치 표시. `data-on` |
| `.hairline` | 구분선 |

## 접근성

- **컨트롤 경계** — `--color-edge`를 면 대비 3.1:1로 측정해 맞췄다 (WCAG 1.4.11 비텍스트 대비).
- **글자가 어포던스인 컨트롤**(내비게이션 항목)은 경계 없이 간다. 텍스트가 4.5:1을 넘기므로 별도 경계가 요구되지 않는다.
- 모든 터치 타겟 44px 이상
- `:focus-visible` 에 2px 포인트 아웃라인, offset 2px
- `prefers-reduced-motion` 이면 전환 시간이 0이 되고 메뉴 등장이 즉시로 바뀐다
- 상단에 본문 건너뛰기 링크

## 타이포그래피

**Pretendard** 한 가족 (Kil Hyung-jin). 가변 굵기 45–920.

- **라이선스**: SIL Open Font License 1.1 — 상업적 사용·웹 임베딩·재배포 모두 허용. 라이선스 전문을 `public/fonts/pretendard/OFL.txt` 에 함께 배포한다 (OFL 조건). Pretendard는 Source Sans·Inter·M PLUS 1을 기반으로 하며 그 저작권 표기가 라이선스 파일에 함께 들어 있다.
- **고른 이유**: Apple SD Gothic Neo와 같은 메트릭으로 설계된 서체다. 지금 화면이 Apple식 면·여백·자간을 기준으로 잡혀 있으므로, 그 위에 올릴 글자로는 가장 정확하게 들어맞는다. 45–920의 연속 가변축은 장식 없는 화면에서 위계를 글자가 짊어지는 폭을 감당한다.
- **배포**: 직접 호스팅. `public/fonts/pretendard/` 에 92개 동적 서브셋(총 3.1MB), `src/app/fonts.css` 가 `unicode-range` 로 건다. 방문자는 **실제로 쓰인 글자가 든 조각만** 내려받는다(개당 ~20–40KB). 외부 CDN 의존이 없다.
- **preload**: Pretendard는 가장 흔한 글자를 마지막 서브셋에 몰아둔다. 라틴 + 최빈 한글이 든 `subset.91`(37KB)과 `subset.90`(20KB) 두 개만 미리 받는다.
- **모노**: 코드와 16진 값에만 `IBM Plex Mono` (OFL, next/font/google).

반지름은 컨트롤 12px, 카드 18px, 트랙 14px.

## 메뉴

- **데스크톱**: 정보 메뉴(소개·솔루션·소식)는 들어간 트랙 안에 있고, 열리거나 현재 위치인 항목만 흰 면으로 올라온다. **온라인 설명회**는 전환 동선이라 트랙 밖에 포인트 컨트롤로 따로 선다.
- **모바일**: 같은 바탕이 이어지고 항목만 흰 면으로 올라온다.
- 호버 60ms 후 열림, 벗어나면 160ms 후 닫힘. 호버는 `(hover: hover) and (pointer: fine)` 기기에서만.
- `↓`로 메뉴 진입, 메뉴 안에서 `↑↓ Home End`, `Esc`로 닫으면 포커스가 열었던 컨트롤로 복귀. Tab으로 빠져나가면 닫힘. 모바일 시트에는 포커스 트랩.

메뉴 구조의 출처는 `src/lib/nav.ts` 하나다. `PRD.md`의 구조를 그대로 옮겼다.

## 수직 리듬

섹션 간 64px, 제목 아래 24px, 카드 내부 24–40px, 요소 간 12px. 4px 배수만 쓴다.

## 남은 것

- 랜딩 첫 화면(히어로) — 애니메이션과 이미지 구성은 별도 기획
- 푸터
- 하위 페이지 본문 (현재 `PageStub` 으로 자리만 잡아둠)
